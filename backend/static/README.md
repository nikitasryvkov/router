This directory is reserved for public static backend resources.

The current site version does not store user submissions, uploaded files,
session data, identifiers, cookies, or analytics payloads here.
